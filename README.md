# todo-api
A RESTful API built with Laravel for managing a Todo List. Features include full CRUD operations, input validation, custom endpoints for task completion, and handling of database indexing limitations.
